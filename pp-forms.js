import{$,esc,cap,S,sb,bill,client,addM,today,toast,load}from'./pp-lib.js';
const close=()=>$('#modal').classList.add('hidden');function modal(h){$('#modalBody').innerHTML=h;$('#modal').classList.remove('hidden');document.querySelectorAll('[data-close]').forEach(x=>x.onclick=close)}
export function clientForm(id,after){let c=id?client(id):{},b=id?bill(id):{},service=c.service_type==='Contest prep'?'contest_prep':c.service_type==='Lifestyle'?'lifestyle':c.service_type||'other';modal(`<div class="modalhead"><h2>${id?'Edit':'Add'} client</h2><button class="btn sm" data-close>Close</button></div><div class="formgrid"><div class="field full"><label>Client name</label><input id="fn" class="input" value="${esc(c.name||'')}"></div><div class="field"><label>Email</label><input id="fe" class="input" value="${esc(c.email||'')}"></div><div class="field"><label>Phone</label><input id="fp" class="input" value="${esc(c.phone||'')}"></div><div class="field"><label>Service</label><select id="fs" class="input"><option value="contest_prep" ${service==='contest_prep'?'selected':''}>Contest prep</option><option value="lifestyle" ${service==='lifestyle'?'selected':''}>Lifestyle</option><option value="other" ${service==='other'?'selected':''}>Other</option></select></div><div class="field"><label>Status</label><select id="fst" class="input"><option value="active">Active</option><option value="paused">Paused</option><option value="completed">Completed</option><option value="inactive">Inactive</option></select></div><div class="field"><label>Billing type</label><select id="fb" class="input"><option value="monthly">Monthly</option><option value="lump_sum" ${b.billing_type==='lump_sum'?'selected':''}>Lump sum</option><option value="free" ${b.billing_type==='free'?'selected':''}>Free</option></select></div><div class="field"><label>Payment method</label><select id="fm" class="input"><option>PayFast</option><option ${b.payment_method==='EFT'?'selected':''}>EFT</option></select></div><div class="field"><label>Monthly equivalent (R)</label><input id="fme" type="number" class="input" value="${b.monthly_equivalent??''}"></div><div class="field"><label>Lump-sum amount (R)</label><input id="fl" type="number" class="input" value="${b.lump_sum_amount??''}"></div><div class="field"><label>Payment / next due date</label><input id="fd" type="date" class="input" value="${b.payment_due_date||''}"><div class="hint">When the next payment is expected.</div></div><div class="field"><label>Package term</label><select id="ft" class="input"><option value="3">3 months</option><option value="6" ${b.term_months==6?'selected':''}>6 months</option><option value="12" ${b.term_months==12?'selected':''}>12 months</option></select></div><div class="field"><label>Package start date</label><input id="fstart" type="date" class="input" value="${b.term_start_date||''}"></div><div class="field"><label>Package end date (auto)</label><input id="fend" type="date" readonly class="input" value="${b.term_end_date||''}"></div><button class="btn full" data-close>Cancel</button><button id="save" class="btn primary full">Save client</button></div>`);let sync=()=>{$('#fend').value=$('#fstart').value?addM($('#fstart').value,Number($('#ft').value)):''};$('#ft').onchange=sync;$('#fstart').onchange=sync;sync();$('#save').onclick=()=>saveClient(id,b.id,after)}
async function saveClient(id,bid,after){let isNew=!id,rec={name:$('#fn').value.trim(),email:$('#fe').value.trim()||null,phone:$('#fp').value.trim()||null,service_type:$('#fs').value,status:$('#fst').value,created_by:S.profile.id};if(!rec.name)return toast('Client name is required.',true);let r=isNew?await sb.from('clients').insert(rec).select().single():await sb.from('clients').update(rec).eq('id',id).select().single();if(r.error)return toast(r.error.message,true);id=r.data.id;let type=$('#fb').value,term=type==='lump_sum'?Number($('#ft').value):null,start=type==='lump_sum'?$('#fstart').value||null:null,br={client_id:id,billing_type:type,term_months:term,plan_label:type==='lump_sum'?term+' month term':cap(type),monthly_equivalent:type==='free'?0:Number($('#fme').value||0),lump_sum_amount:type==='lump_sum'?Number($('#fl').value||0):null,payment_method:$('#fm').value,payment_due_date:type==='free'?null:$('#fd').value||null,term_start_date:start,term_end_date:start?addM(start,term):null,active:true};let q=bid?await sb.from('client_billing').update(br).eq('id',bid):await sb.from('client_billing').insert(br);if(q.error){if(isNew)await sb.from('clients').delete().eq('id',id);return toast(q.error.message,true)}close();await load();after();toast('Client saved.')}
export async function markPaid(id,after){let b=S.billing.find(x=>x.id===id),amt=b.billing_type==='lump_sum'?Number(b.lump_sum_amount):Number(b.monthly_equivalent);let r=await sb.from('payments').insert({client_id:b.client_id,billing_id:b.id,due_date:b.payment_due_date,received_date:today(),amount:amt,method:b.payment_method,payment_type:b.billing_type,status:'paid',recorded_by:S.profile.id});if(r.error)return toast(r.error.message,true);await sb.from('client_billing').update({payment_due_date:addM(b.payment_due_date||today(),b.billing_type==='lump_sum'?Number(b.term_months||1):1)}).eq('id',id);await load();after();toast('Payment recorded.')}
export function addEvent(after){modal(`<div class="modalhead"><h2>Add event</h2><button class="btn sm" data-close>Close</button></div><div class="formgrid"><div class="field full"><label>Event name</label><input id="et" class="input"></div><div class="field"><label>Type</label><select id="ey" class="input"><option value="show">Show</option><option value="travel">Travel</option><option value="business">Business</option><option value="personal">Personal</option><option value="deadline">Deadline</option><option value="client">Client check-in</option></select></div><div class="field"><label>Date</label><input id="ed" type="date" class="input"></div><div class="field"><label>Time (optional)</label><input id="etime" type="time" class="input"></div><div class="field full"><label>Location</label><input id="el" class="input"></div><button id="es" class="btn primary full">Save event</button></div>`);$('#es').onclick=async()=>{let day=$('#ed').value,time=$('#etime').value,title=$('#et').value.trim();if(!title||!day)return toast('Event name and date are required.',true);let r=await sb.from('calendar_items').insert({title,item_type:$('#ey').value,start_at:new Date(day+'T'+(time||'12:00')).toISOString(),all_day:!time,location:$('#el').value.trim()||null,visibility:'private',assigned_profile_id:S.profile.id,created_by:S.profile.id});if(r.error)return toast(r.error.message,true);close();await load();after();toast('Event saved.')}}
export function addTask(after){modal(`<div class="modalhead"><h2>Add task</h2><button class="btn sm" data-close>Close</button></div><div class="formgrid"><div class="field full"><label>Task</label><input id="tt" class="input"></div><div class="field"><label>Due date</label><input id="td" type="date" class="input"></div><div class="field"><label>Priority</label><select id="tp" class="input"><option value="medium">Medium</option><option value="high">High</option><option value="urgent">Urgent</option><option value="low">Low</option></select></div><button id="ts" class="btn primary full">Save task</button></div>`);$('#ts').onclick=async()=>{let title=$('#tt').value.trim();if(!title)return toast('Task name is required.',true);let r=await sb.from('tasks').insert({title,due_date:$('#td').value||null,category:'business',priority:$('#tp').value,status:'todo',visibility:'private',assigned_profile_id:S.profile.id,created_by:S.profile.id});if(r.error)return toast(r.error.message,true);close();await load();after();toast('Task saved.')}}